# Neon database connection string
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# (Optional) Secret key to protect the reset database endpoint
RESET_DB_SECRET="your-super-secret-key-here"
